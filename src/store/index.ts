import { createStore } from 'vuex';
import menuRoutes from '@/composables/menuRoutes';

export default createStore({
  state: {
    menuRoutes,
  },
  mutations: {},
  actions: {},
  modules: {},
});
